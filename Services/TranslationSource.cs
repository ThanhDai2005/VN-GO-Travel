namespace MauiApp1.Services;

/// <summary>
/// Identifies which app path initiated a translation request (orchestrator logging only).
/// </summary>
public enum TranslationSource
{
    PoiFocus,
    Narration
}
